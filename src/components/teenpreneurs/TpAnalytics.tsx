/**
 * ──────────────────────────────────────────────────────────────
 * ANALYTICS SLOT — the single place to add tracking snippets.
 *
 * When ready to run ads, paste here (as <Script> from 'next/script',
 * strategy="afterInteractive"):
 *   1. Your analytics snippet (e.g. Plausible / GA4)
 *   2. The Meta (Facebook) Pixel for event + shop campaigns
 *
 * Keep it inside this component so consent handling and future
 * locale-specific rules stay in one file.
 * ──────────────────────────────────────────────────────────────
 */
export function TpAnalytics() {
  return null
}
