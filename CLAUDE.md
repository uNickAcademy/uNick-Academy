# Deployment

Production (unick-academy.pl) deploys automatically from the `main` branch via Vercel.

Per explicit user instruction, push finished/tested work directly to `main` without asking for confirmation each time — this includes fast-forwarding a feature branch onto `main`. Still run `npm run build` first to make sure it compiles cleanly.
